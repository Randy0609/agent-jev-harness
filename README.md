# Agent + JEV Harness

**给你的 AI 助手一套可检查的工作流程。**

如果你已经让 AI 做日报、查资料、整理客户需求或制作方案，你可能也有这样的困惑：

“它看起来什么都懂，为什么最后还是要我一项项检查？”

这个项目想和你一起解决这个问题：把任务里的关键判断、检查要求和完成依据讲清楚，再通过 JEV 和程序把其中一部分接进 AI 的工作流程。

本项目回答三个问题：**为什么需要？它是什么、为什么这样设计？企业的经营者（创业者、老板）怎么用？**

这是一个可运行的早期项目。当前提供开工分流、交付证据摘要复核与合成演示；企业实际效果仍待验证。

仓库直接提供实现这些能力的 Python 源码、MCP / 命令行入口、依赖清单和测试。经营者可以先读说明，再把同一个仓库交给自己的 Agent 或技术伙伴检查并协助接入。

## 1. 为什么需要？从一句“已经完成”说起

假设你让 AI：

> 帮我整理昨天的经营日报，核对数据，并发送到指定工作群。

它回复“完成了”。你最关心的几个问题，却可能还没有答案：

- 用的是昨天的完整数据吗？
- 表里的数字核对过吗？
- 日报只是生成了，还是已经发送？
- 工作群里实际能看到吗？

这说明，给 AI 一个目标之外，还需要一套可检查的工作流程：哪些事实必须核对，什么情况需要补查，用什么证据证明完成。

Harness 就是承载这些要求的机制。JEV 则可以在其中回答一些范围清楚的小问题，例如“这项完成声明是否被提供的证据支持”。

**我们希望减少的是任务中的遗漏和返工。** 是否有效，要看接入前后的实际结果。

并非所有工作都要增加 JEV：写文案、自由讨论未必需要；金额加总、日期计算、订单精确匹配应优先由普通程序完成。

## 2. JEV Harness 是什么？为什么这样设计？

先记住三个部分：

| 部分 | 用经营者熟悉的话理解 | 实际负责什么 |
|---|---|---|
| Agent | 你的 AI 助手 | 理解任务、查资料、使用工具、制作结果 |
| JEV | 回答小问题的判断员 | 在给定信息和明确选项内给出判断与概率信号 |
| Harness | 一套工作规范和检查机制 | 规定何时判断、怎样检查、出了问题怎么办 |

**Agent + JEV Harness，就是让 AI 助手在既定流程中使用 JEV 的判断能力，并由程序检查和处理结果。** 经营者仍负责业务目标、授权范围和验收标准。

### 为什么让 JEV 参与？

写一份方案，需要组织语言和综合理解；判断一条证据是否支持某项声明，可能只需要在“支持、不支持、矛盾”中选择。

JEV 提供适合后者的类型化判断接口，返回明确的结果和概率信号，便于程序处理。接口形态可查阅 [TypeSafe 官方说明](https://docs.typesafe.ai/concepts/system-one)。

我们因此尝试把它用于一些小判断。但“多一个模型”不自然等于“更准确”：JEV 也会出错，需要与规则检查、原有 AI 的结构化输出一起比较，验证有没有额外价值。

### Harness 为什么不能只是一段提示词？

“认真检查，不要犯错”表达了愿望。具体流程还需要处理：

- 没有证据，不能当成检查通过。
- 返回格式不合要求，不能直接交给后续程序。
- 判断不明确，需要继续核实。
- 服务超时，必须说明本次没有完成 JEV 检查。
- 任何判断结果，都不能替代发消息、付款或发布的原有授权。

本项目同时提供 Agent 指引和程序检查。指引帮助 Agent 选择何时调用；程序负责它能确定检查的部分。当前仍可能漏调用，尚未强制拦截全部工具操作。

### 第一版怎样帮助 AI 执行任务？

```text
你交代任务
    ↓
开工前：JEV 建议处理路线、提示可能缺少的条件
    ↓
Agent 执行工作，读取原始资料和工具结果
    ↓
交付前：JEV 对照“完成声明”和“证据摘要”
    ↓
Agent 核对疑点、补做工作，或明确说明未完成部分
    ↓
你查看结果和依据
```

JEV 看的是提供给它的摘要，不会亲自打开你的业务系统。摘要错了、漏了，它也可能判断错。因此实际计算、事实查询和结果验证仍需由程序、Agent 或人完成。

## 3. 企业的经营者怎么用？

实际使用从获取 API Key 开始：先取得调用凭据，再完成配置和连通验证，最后选择一件具体工作试用。安装与配置可以由具备工具能力的 Agent 或技术伙伴协助完成。

### 第一步：去 JEV 官方网站（或兼容的中转站）获取 API Key

API Key 可以理解为让你的 AI 调用 JEV 服务的访问凭据。**要实际调用 JEV，先准备好自己的 Key。**

推荐先使用官方渠道：

1. 打开 [JEV / TypeSafe 官网](https://typesafe.ai/)，进入控制台并注册或登录。
2. 打开 [API Keys 页面](https://console.typesafe.ai/keys)，创建自己的 API Key，并妥善保存。
3. 查看账户当前的调用额度和计费要求，确认可以使用后再接入。[官方入门说明](https://docs.typesafe.ai/introduction/quickstart)。

如果选择中转站，需要同时取得它提供的 **API Key、接口地址和可用模型名称**，并确认其支持 JEV 的请求与返回格式。**当前仓库代码固定连接 TypeSafe 官方接口，尚未适配或验证任何中转站；仅把中转站 Key 填进来不能保证可用。** 中转接入需由技术伙伴适配和验证，任务摘要也会经过该服务。

不要把 Key 贴到 GitHub、公开聊天或截图中。通过本地环境变量 `TYPESAFE_API_KEY` 或凭据文件配置，具体见[接入指南](docs/integration.md)。只看文档或运行离线演示不需要 Key。

### 第二步：让自己的 AI 或技术伙伴完成配置，并验证能调用

把项目地址交给具备工具能力的 AI 或技术伙伴，可以这样说：

> 我已准备好 JEV API Key。请阅读这个项目的接入指南，检查当前 AI 是否支持相关工具，并在现有授权范围内协助配置。Key 通过本地凭据方式提供，不要把它写进项目或对话。请先用合成示例验证真实调用，展示是否成功、实际模型与返回结果，再接入我的业务任务。如果使用中转站，请先核对接口兼容性和需要的适配。

配置后，应完成一次真实合成调用并读取返回结果。工具已安装、Key 已保存或本地 status 正常，都不能单独证明已经连通。不同 AI 产品的工具支持与配置方式不同，需要在目标环境实际验证。

### 第三步：写清楚自己的工作

| 你需要说清楚 | 以经营日报为例 |
|---|---|
| 想得到什么结果 | 一份昨天的经营日报 |
| 用哪些资料 | 指定业务系统中昨天完整的数据 |
| 哪些事实必须核对 | 日期、覆盖范围、金额与合计 |
| 哪些动作可以执行 | 先生成供我检查；发送需按既有授权 |
| 怎样证明完成 | 文件能读、数字可核对；如要求发送，需相应回执 |

这些要求由经营者决定，不能让模型凭空替你补齐。

### 第四步：看懂一个完整示例

以下是**教学示意，不是真实企业效果报告**：

| 阶段 | 发生什么 |
|---|---|
| 任务要求 | 生成日报，并在获准后发送 |
| 实际证据 | 已读取生成的文件；没有发送回执 |
| 待检查声明 | “日报已生成并送达” |
| 检查提示 | “已生成”有依据；“已送达”缺证据 |
| 下一步 | Agent 查询发送状态，或清楚交代“已生成，发送情况待核验” |

其中，“回执是否存在”可以由程序直接检查。JEV 可补充判断较复杂的声明与证据含义是否匹配；不应把每一项检查都交给模型。

仓库中的离线演示使用预设响应，帮助你理解这个流程；真实模型调用另有明确标记。

### 第五步：用小试点判断价值

先保留原有处理方式，让新增检查给建议。由负责人核对：它发现了什么遗漏？误拦了哪些正常结果？你少介入了几次？增加了多少时间和费用？

如果没有收益，就缩小或停用这一环节；如果确实有价值，再扩大到相似工作。

## 可以从哪些工作开始？

| 工作 | 可能有用的小判断 | 当前状态 |
|---|---|---|
| 日报、研究报告、项目交付 | 完成声明是否有相应证据 | 通用接口已实现；有合成日报示例 |
| 客服工单整理 | 诉求属于哪一类，是否表达明确请求 | 后续设计，业务功能未实现 |
| 销售线索整理 | 是否明确表达采购需求、数量或时间 | 后续设计，业务功能未实现 |

开始时优先选结果容易核对、错误可以纠正的工作。

## 你需要准备什么？

- 一件具体工作和一位能验收结果的负责人。
- 一个支持所需工具的 Agent，或能够协助接入的技术伙伴。
- 合成样例或允许用于试点的脱敏资料。
- 真实调用时，自己的 TypeSafe 凭据与可接受的调用费用。

开源代码采用 MIT 许可；第三方模型服务不包含在免费源码里。不要把密钥、客户信息或私有账本提交到 GitHub。

## 目前做到了什么？

已实现开工分流、交付证据摘要复核，以及供技术伙伴使用的 MCP / 命令行入口。已有 17 项核心测试和两个真实合成调用的验证记录。

这些证明初版能够运行，不证明能提高所有企业任务的准确率，也不证明兼容所有 AI 产品。目前没有长期企业效果评估，没有自动接通你的业务系统。

## 从这里开始

- [三分钟介绍](docs/for-founders.md)
- [企业试点任务卡](docs/sme-playbook.md)
- [接入指南](docs/integration.md)与[Agent 技能指引](skills/agent-jev-harness/SKILL.md)
- [验证记录](docs/verification.md)与[效果评估方法](docs/evaluation.md)
- [源码核心](core.py)、[MCP / CLI 入口](server.py)与[测试](test_core.py)


经营者的实际使用顺序：获取 API Key → 配置并验证真实调用 → 明确工作与验收要求 → 小范围试用。

技术伙伴再看：接入指南 → 合成调用 → 目标 Agent 内验证 → 试点记录。

欢迎通过脱敏或合成案例提出问题：你的 AI 在什么任务上需要你反复检查？什么证据才会让你认为它真正完成？

## 下载代码并运行演示

需要 Python 3.11+。在终端中运行：

```bash
git clone https://github.com/Randy0609/agent-jev-harness.git
cd agent-jev-harness
python3 demo.py
python3 -m unittest -v
```

上述演示使用预设响应，不需要密钥，也不调用外部模型。真实接入需要独立安装依赖、配置自己的 TypeSafe 凭据，详见接入指南。

## 仓库里有哪些代码？

| 文件 | 用途 |
|---|---|
| [core.py](core.py) | JEV 请求、输出校验、分流、复核、调用额度与审计 |
| [server.py](server.py) | MCP 工具和 JSON 命令行入口 |
| [demo.py](demo.py) | 离线预设响应演示 |
| [test_core.py](test_core.py) | 核心行为与失败分支测试 |
| [verify_mcp.py](verify_mcp.py) | MCP 验证；显式使用 --live 才进行真实合成调用 |
| [requirements.txt](requirements.txt) | 直接依赖；[完整版本记录](requirements.lock) |
| [examples](examples) | 合成任务与证据输入 |
| [skills](skills) | 供 Agent 采用的工作指引 |

当前 v0.1.0 是由 Agent 指引触发的辅助 Harness，没有强制拦截全部工具调用。返回 no_gap_detected 只说明提供的摘要未被判定存在缺口，不是对原始业务系统的独立验收。

这是独立社区项目，与 TypeSafe 无官方隶属关系。MIT 许可覆盖本仓库的 Harness 源码、示例和原创文档；不包含 JEV 模型、供应商服务或第三方依赖。

[MIT 许可](LICENSE) · [贡献指南](CONTRIBUTING.md) · [安全与数据边界](SECURITY.md) · [事实口径](docs/claims.md) · [创始人演讲精读](docs/founder-talk-reading.md)
