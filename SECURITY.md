# 安全与数据边界

本服务只处理调用者明确传入的摘要，不自动读取业务目录或完整会话。它会读取明确配置的凭据与本地运行状态。外部接收方是 TypeSafe；不要把不能外发的数据送入 state。

常见凭据模式检查、邮件和手机号替换是辅助措施，不构成完整的数据防泄漏系统。摘要可能仍包含姓名、商业信息或独特事件。运行前由调用者最小化数据。

本服务没有业务工具执行权限，但消耗 API 额度并写入本地审计。提示中的“忽略嵌入指令”无法保证抗提示注入；输出允许列表与确定性校验只能限制接口，不能保证语义正确。

不要在公开 issue 中提交可用凭据或私有数据。正式仓库发布后使用其私密漏洞报告入口；尚无入口时先移除敏感内容，只提供合成复现步骤。此初版尚未经过独立安全审计。

## 可选的五组件集成

上文的摘要脱敏、固定 TypeSafe 接口和调用限额指原有 core.py。新增 integrations 服务是独立入口：可选择 TypeSafe/OpenRouter，只传入所选 Key；不继承 core.py 的脱敏器、100 次限制或 DISABLED 文件。`data_class` 是调用者声明，调用前仍须最小化输入，另行控制费用。

默认不安装 Canny hooks，不运行上游自动设置或更新，不复制全局环境变量。上游 Canny CLI 可以主动启用 hooks；其采集和外发范围与本项目台账示例不同，启用前应阅读上游说明。第三方源代码和二进制经过版本固定及相关测试，并不等于完成独立安全审计。

验收页面在本地读取 JSON，按受限组件渲染文本；不会执行 JSON 中的代码或业务动作。页面显示的是回执声明，不是对来源系统或台账防篡改性的独立认证。
