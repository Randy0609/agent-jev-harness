# 贡献指南

欢迎先提交具体问题：目标是什么，输入是什么，预期与实际如何不同。使用合成或公开样例，附 Python 版本、模型版本、复现命令和测试结果。

修改核心判断或校验逻辑时，增加能复现真实失败的测试。运行 `python3 -m unittest -v` 与 `python3 demo.py`。MCP 变更另运行 `python verify_mcp.py`；真实 API 检查须显式选择 `--live`，不要加入默认 CI。

提交前检查 diff 不含密钥、客户资料、账本、私人路径、聊天截图或个人运行记录。增加外部依赖时说明用途与许可证。贡献按项目 MIT 许可提供，供应商 API 仍受其服务条款约束。
